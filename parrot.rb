# Create method `parrot` that outputs a given phrase and
# returns the phrase


def parrot(word = "Squawk!") #def oarrot method accepting word as argument with defaul of "Squawk!"
puts word #display string entered or if none the default
word #return the string at the end of the argument as return for puts command is always nil
end
